<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">All Posts</h1>
        <a href="/add-post" class="btn btn-primary mb-3">Create New Post</a>
        <ul class="list-group">
            <% posts.forEach(post => { %>
                <li class="list-group-item d-flex flex-column justify-content-between align-items-start">
                    <h5><a href="/post?id=<%= post.id %>"><%= post.title %></a></h5>
                    <p><small><%= post.content.slice(0, 100) %>...</small></p>
                </li>
            <% }); %>
        </ul>
    </div>
</body>
</html>